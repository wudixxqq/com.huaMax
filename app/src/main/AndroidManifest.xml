<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <application
        android:name=".manager.App"
        android:icon="@mipmap/ic_launcher"
        android:description="@string/description"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.LocationMax"
        tools:targetApi="31">
        <activity
            android:name=".manager.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LocationMax">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".manager.mock.MockLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!--
            Off by default. The user opts in via Settings → External Control,
            which calls PackageManager.setComponentEnabledSetting(...) to flip
            this receiver to ENABLED at runtime. Until then, no external app
            (and no `adb shell am broadcast`) can reach it.
        -->
        <receiver
            android:name=".manager.control.ControlReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="com.huaMax.action.START" />
                <action android:name="com.huaMax.action.STOP" />
                <action android:name="com.huaMax.action.SET_LOCATION" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
